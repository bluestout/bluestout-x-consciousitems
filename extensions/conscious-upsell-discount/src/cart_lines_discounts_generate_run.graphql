query CartInput {
  cart {
    lines {
      id
      quantity
      cost {
        amountPerQuantity {
          amount
        }
      }
      upsellPrice: attribute(key: "_upsell_price") {
        value
      }
    }
  }
  discount {
    discountClasses
  }
}
